package pages;

import org.openqa.selenium.WebDriver;
//Header element (shopping cart button and menu button ) корзина. меню
public class HomePage extends BasePage {
    public HomePage(WebDriver driver){
        super(driver);
    }
}
